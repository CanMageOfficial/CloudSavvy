# Copy this file to .env.<environment> (e.g. .env.dev, .env.prod, .env.staging)
# These files are gitignored — never commit them.

BUCKET_NAME=lambda-artifacts-xxxxxxxxxxxxxxxx
AWS_PROFILE=default
AWS_REGION=us-east-1
EMAIL_ADDRESS=your@email.com
CREATE_SNS_TOPIC=false
